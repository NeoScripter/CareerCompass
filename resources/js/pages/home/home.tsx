import AppLayout from '@/layouts/app-layout';
import HeroSection from './partials/hero-section';

const Home = () => {
    return (
        <AppLayout>
            <HeroSection />
        </AppLayout>
    );
};

export default Home;
