import { NodeProps } from '@/types/nodeProps';
import { FC } from 'preact/compat';

const ResultJobs: FC<NodeProps> = ({ className }) => {
    return <section>Result jobs</section>;
};

export default ResultJobs;
